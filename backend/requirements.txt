# ── Core API ──────────────────────────────────────────────────────────────────
fastapi==0.104.1
uvicorn[standard]==0.24.0
starlette>=0.27.0,<0.28.0
anyio>=3.7.1,<4.0.0
python-multipart==0.0.6
aiofiles==23.2.1
python-dotenv==1.0.0
pydantic>=2.9.0
pydantic-settings>=2.5.0

# ── LangChain (RAG / chatbot) ─────────────────────────────────────────────────
langchain==0.3.11
langchain-community==0.3.11

# ── Vector store + embeddings ─────────────────────────────────────────────────
faiss-cpu>=1.9.0
sentence-transformers>=2.7.0
nltk>=3.8.0

# ── LLM ───────────────────────────────────────────────────────────────────────
groq>=0.4.0

# ── Speech (lightweight only) ─────────────────────────────────────────────────
# gTTS = simple Google Text-to-Speech, very small
gtts>=2.5.0
# openai-whisper is HUGE (pulls torch ~2GB). Only uncomment if you use voice input.
# openai-whisper>=20231117

# ── Translation ───────────────────────────────────────────────────────────────
deep-translator==1.11.4
httpx>=0.23.0,<1.0.0

# ── Utilities ─────────────────────────────────────────────────────────────────
requests>=2.31.0
numpy>=1.24.0,<3.0.0
pypdf==3.17.4
python-docx==1.1.0
click>=8.0.0

# ── Flask (for fertilizer_app.py and crop_app.py) ────────────────────────────
flask>=3.0.0
flask-cors>=4.0.0
werkzeug>=3.0.0

# ── ML for crop disease model (smaller than torch) ───────────────────────────
# tensorflow is large (~600MB) but needed for crop_app.py's .h5 model
# If you don't use crop disease feature, comment these out:
tensorflow>=2.13.0
scikit-learn>=1.3.0
lightgbm>=4.0.0
pillow>=10.0.0
pandas>=2.0.0
